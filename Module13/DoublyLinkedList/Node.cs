namespace ZbW.ProgrammingFoundation.Lessons.Module13.DoublyLinkedList
{
    /// <summary>
    /// Repräsentiert einen einzelnen Knoten in einer doppelt verketteten Liste.
    /// Im Gegensatz zur SinglyLinkedList kennt jeder Knoten auch seinen Vorgänger.
    /// </summary>
    public class Node
    {
        // TODO: Property für den gespeicherten Wert (Typ: object)

        // TODO: Property für den Zeiger auf den nächsten Knoten (Typ: Node)

        // TODO: Property für den Zeiger auf den vorherigen Knoten (Typ: Node)
    }
}
